interface:
  display_name: "GitHub Launch Kit"
  short_description: "Polish a repo for launch, clarity, and community traction."
  default_prompt: "Use $github-launch-kit to turn this repository into a launch-ready open-source project with a sharper README, clearer positioning, and better GitHub presentation."
policy:
  allow_implicit_invocation: true
